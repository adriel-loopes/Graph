# templates/NOTES.txt

Thank you for deploying {{ .Release.Name }}!

To get started:
1. Run `kubectl get pods` to verify your app is running.
2. Access your application at the following URL: http://{{ .Values.service.host }}:{{ .Values.service.port }}.
